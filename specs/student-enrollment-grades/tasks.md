# Tasks — student-enrollment-grades

- [x] T1 — Backend: extraer `App\Services\Grade\FinalGradeCalculator` desde `GradeCardResource::calculateFinalGrade` (misma lógica, sin cambio de comportamiento). Actualizar `GradeCardResource` para usarlo. Regresión: `tests/Feature/Professor/GradeCalculationTest.php` debe seguir pasando igual.
- [x] T2 — Backend: método de promedio de periodo (en el mismo servicio o uno nuevo cohesivo) que dado un `Enrollment` cargado devuelve el promedio simple de `final_grade` no nulos por materia, `null` si ninguna tiene nota.
- [x] T3 — Backend: `StudentController::show` — agregar `details.subject`, `details.gradeEntries.children` al eager load de `enrollments`. `StudentShowResource` — agregar `period_average` a cada `enrollments[]`, y calcular `cumulative_gpa` como promedio simple de esos `period_average` no nulos (dejar de leer la columna estática).
- [x] T4 — Backend: ruta `GET academic/students/{student}/enrollments/{enrollment}` (`students.enrollments.show`) + `StudentController::showEnrollment` (404 si no pertenece al estudiante, `authorize('view', $enrollment)`, reusa `GradeCardResource` con `GradeVisibility::RealTime`).
- [x] T5 — Frontend: `types/studentShow.ts` — agregar `period_average: string | null` a `EnrollmentHistoryItem`.
- [x] T6 — Frontend: `admin/Students/Show.vue` — columna "Promedio del periodo" + link "Ver detalle" (Wayfinder) en la tabla de historial de inscripciones.
- [x] T7 — Frontend: nueva página `admin/Students/EnrollmentGrades.vue` (reusa tipos de `types/grade-entry.ts`, estilo Tailwind consistente con `Show.vue`).
- [x] T8 — Tests: `tests/Feature/Academic/StudentShowPeriodAverageTest.php` (creado en lugar de extender `StudentControllerTest.php`, que solo cubre el índice — ver Deviations) — casos de `period_average`/`cumulative_gpa` con notas completas, parciales y sin notas. Nuevo `tests/Feature/Academic/StudentEnrollmentGradesTest.php` — 200 + datos, 404 si la inscripción no pertenece al estudiante de la ruta, admin ve cualquiera.
- [x] T9 — `vendor/bin/sail bin pint --dirty --format agent` + `vendor/bin/sail artisan test --compact --filter=Student` (incluye `StudentControllerTest`, `StudentShowAcceptanceTest`, `StudentEnrollmentGradesTest`, `GradeCalculationTest`) — todo verde.

## Fixes post-revisión (4R: risk, resilience, readability, reliability)

- [x] T10 — Fix BLOCKER: `students.enrollments.show` permitía al estudiante dueño/representante ver notas `RealTime` saltándose `grade_visibility` del equipo. Restringido a `role:Admin` en `routes/web.php`. Tests: 403 para estudiante dueño y para representante (rojo→verde confirmado).
- [x] T11 — Fix BLOCKER (bug preexistente, no introducido por esta feature): `GradeConfigResolver::resolveForDetail` comparaba `EducationalLevel` (`primary`/`secondary`/`university`) contra `GradeConfig.level` (`GradeLevel`: `primary_secondary`/`university`) — nunca calzaba para primaria/bachillerato, notas finales siempre null. Corregido con `match()`. Afectaba también al portal de notas del estudiante/representante (mismo resolver compartido). Tests unitarios nuevos (rojo→verde confirmado) + caso feature con estudiante `secondary`.
- [x] T12 — Fix WARNING (corroborado por 3 revisores): N+1 en `GradeConfigResolver` — memoización por instancia agregada, sin cambio de comportamiento observable.
- [x] T13 — Fix SUGGESTION: variable `$definitiva` renombrada a `$weightedTotal` (convención de código en inglés).
- [x] T14 — Fix BLOCKER (RISK-001, corroborado por reliability como R3-001-relacionado): `academic.students.show`, `academic.students.index`, `academic.guardians.show`, `academic.guardians.index` en `routes/web.php` solo tenían middleware `['auth', 'verified']` — cualquier usuario autenticado (profesor, estudiante, representante sin relación) podía ver el perfil académico completo de cualquier estudiante o representante, incluyendo `cumulative_gpa`/`period_average` agregados por esta feature. Restringido a `role:Admin` (mismo subgrupo que ya envolvía `students.enrollments.show`), acorde a la intención de diseño confirmada en `AppSidebar.vue` (links "Estudiantes"/"Representantes" solo visibles para rol Admin). Tests nuevos en `StudentControllerTest.php` y `GuardianControllerTest.php` (403 para rol Profesor/Estudiante sin relación — rojo→verde confirmado). Tests existentes que usaban un actor sin rol (`StudentControllerTest.php`, `GuardianControllerTest.php`, `StudentShowAcceptanceTest.php`, `StudentShowPeriodAverageTest.php`) actualizados para asignar rol Admin al actor, sin relajar el fix.
